import editor
import platform 

from editor import root
from editor import *
from editor.colourx import *
if platform.system() == 'Windows':
    if platform.release() == '7'
        print('Windows 7 is not supported, please install an older version of surf-exel ')
        root.mainloop()
    else:
        root.mainloop()
else:
    root.mainloop()
